# ตรวจเส้นทาง/เมนู (Route/Menu Audit)

ใช้คู่กับหน้า `/admin/health` และ `bun run e2e:smoke` เพื่อตรวจว่าเมนูหลักในเว็บมีผลลัพธ์จริง ไม่ใช่ปุ่มหลอกก่อนดีพลอย

| พื้นที่ | Route | ปุ่ม/เมนู | ผลลัพธ์จริง | เงื่อนไขปิดปุ่ม/Guard | สถานะว่าง |
| --- | --- | --- | --- | --- | --- |
| สำรวจ / หน้าแรก | `/` | ค้นหา, หมวดหมู่, การ์ดตัวละคร, เล่นต่อ, สร้างตัวละคร, เมนูล่างมือถือ | เปิดหน้าตัวละคร, เปิดแชทเดิม, ไปหน้าสร้างตัวละครได้จริง และมือถือมีเมนูไปหน้าหลักครบ | ถูกปิดเฉพาะตอนโหลดข้อมูล | มีข้อความโหลดผิดพลาดและการ์ดตัวอย่างกันหน้าว่าง |
| หน้าโปรไฟล์ตัวละคร | `/characters/:characterId` | สัญญาความสัมพันธ์, เริ่มแชท, คัดลอกลิงก์, รายงาน | ส่ง `relationship_seed` ไปหน้าแชทและเปิดหน้าต่างรายงาน | รายงานถูกปิดระหว่างส่ง | มีข้อความบอกเมื่อโหลดตัวละครไม่ได้ |
| ห้องแชท | `/chat`, `/chat/:chatId` | ช่องพิมพ์, แถบเครื่องมือ, ฉากที่รออยู่, โหมดอ่าน, รายงาน, โปรไฟล์ | ส่งข้อความ, เปิดพรอมป์ฉาก, โหมดอ่านเปลี่ยนพื้นที่อ่านข้อความจริง, รายงาน, ทางไปกระเป๋า/โปรไฟล์ | ปุ่มส่งถูกปิดเมื่อข้อความว่าง, กำลังสตรีม, หรือโทเคนไม่พอ | ห้องใหม่มีบทนำ/คำทักทาย |
| แถบแชท | `/chat` | เมนูสามจุด: แก้ไขแชท, ปักหมุด/ถอนหมุด, จัดเก็บ, เลือก, ลบ | ทุกคำสั่งมีผลจริง ลบต้องยืนยันก่อน และเมนูท้ายแถบเปิดขึ้นด้านบนเพื่อไม่ให้โดนตัด | ไม่มีปุ่มที่ถูกปิดถาวร | ถ้าไม่มีแชทจะแจ้งว่ายังไม่มีแชท |
| สร้างตัวละคร (Creator Studio) | `/create` | AI สร้างรูป+เนื้อหา, อัปโหลด, ลิงก์รูป, ตัวช่วยเช็กแท็ก, ตัวลองบท, เผยแพร่ | สร้างร่างได้จริง มีภาพตัวอย่างสำรองถ้ายังไม่มีผู้ให้บริการรูปภาพ | เผยแพร่ถูกปิดเมื่อข้อมูลหลักไม่ครบ, แท็กชนระดับอันตราย, หรือกำลังบันทึก | แผงความพร้อมบอกช่องที่ยังขาด |
| กล่องแชท | `/chats` | ค้นหา, ตัวกรอง, เปิดแชท, ป้ายฉากที่รออยู่, เมนูสามจุด, เลือกหลายแชท, รีเฟรชรายการ, กู้คืนแชท | เปิดกลับไป `/chat/:chatId` ได้ เมนูแก้ชื่อ/ปักหมุด/จัดเก็บ/ลบมีผลจริง จัดเก็บ/กู้คืน/ลบหลายรายการถูก smoke บนจอเดสก์ท็อปและมือถือแล้ว | ถูกปิดเฉพาะตอนกำลังบันทึกหรือยังไม่ได้เลือกแชท และมี title ภาษาไทยบอกเหตุผล | แต่ละตัวกรองมีสถานะว่างของตัวเอง เช่น ยังไม่มีแชทปักหมุด/จัดเก็บ; ถ้าระบบหลังบ้านยังไม่ตอบจะแสดง `ยังโหลดรายการแชทไม่ได้` พร้อมปุ่มรีเฟรชรายการ |
| กล่องอีเวนต์ | `/events` | ฉากที่รออยู่แบบจัดกลุ่มตามฉาก พร้อมแชทย่อยแต่ละห้อง | กดแชทย่อยในกลุ่มอีเวนต์แล้วเปิดกลับเข้าห้องที่มีฉากรออยู่ | อีเวนต์หมดอายุไม่แสดงคำสั่งหลัก | บอกว่ายังไม่มีฉากสำคัญ |
| โปรไฟล์ / ตัวตนผู้เล่น | `/profile` | ตัวตนผู้เล่น, ตั้งค่าเนื้อหา, ตัวละครที่สร้าง/ชื่นชอบ | เซฟตัวตนผู้เล่นลงบัญชีและร่างในเครื่อง พร้อมใช้เป็นบริบทในพรอมป์แชท ส่วนโหมดเนื้อหาบันทึกผ่านระบบหลังบ้าน | ถูกปิดตอนกำลังบันทึก | บอกให้เริ่มสร้าง/สำรวจเมื่อยังไม่มีข้อมูล |
| กระเป๋าโทเคน | `/wallet` | รีเฟรช, คีย์แอดมิน, เพิ่ม/หักโทเคน, ประวัติใช้งาน, ประวัติธุรกรรม | โหลดโทเคน/ธุรกรรมจากระบบหลังบ้าน และมีข้อมูลทดสอบ QA | รีเฟรชบอกว่ากำลังโหลด; เพิ่ม/หักถูกปิดพร้อมเหตุผลถ้าไม่มีคีย์แอดมิน, ไม่มีสรุปผู้ใช้, จำนวนไม่ถูกต้อง, หรือกำลังส่ง | บอกว่ายังไม่มีการใช้งาน/ธุรกรรม |
| ดูแลรายงาน (Moderation) | `/moderation` | คีย์แอดมิน, ตัวกรอง, ค้นหา, เปิดต้นทาง, คำสั่งจัดการ, audit log | ตรวจคิวรายงานและ audit log ได้ | รีเฟรชบอกให้บันทึก `ADMIN_API_KEY` หรือกำลังโหลด; คำสั่งจัดการบอกว่ากำลังอัปเดต, รายงานปิดงานแล้ว, หรือข้อความถูกจัดเก็บแล้ว | บอกวิธีทดสอบ flow รายงาน |
| ตรวจระบบผู้ดูแล | `/admin/health` | รีเฟรชสถานะ, สรุปตัวกั้นขึ้นใช้งานจริง, เช็กลิสต์ดีพลอย, ตรวจเส้นทาง/เมนู, ลิงก์ตรวจพรอมป์, ลิงก์ทดสอบคุณภาพ | เห็น DB, AI, Supabase, storage, CORS, จำนวนตัวกั้นขึ้นใช้งานจริง, route audit, ขั้นต่อไปของแต่ละตัวกั้น และคำสั่งด่านสุดท้ายในหน้าเดียว พร้อมไปตัวตรวจพรอมป์/ชุดทดสอบอัตโนมัติได้ | ไม่มีปุ่มที่ถูกปิดถาวร รีเฟรชถูกปิดได้เฉพาะตอนเรียกข้อมูล | ระบบหลังบ้านล่มจะแสดงสิ่งที่ต้องแก้ |
| ตัวตรวจพรอมป์ | `/admin/prompt-inspector` | คีย์แอดมิน, เลือกตัวละคร, ข้อความปัจจุบัน, ข้อความก่อนหน้า, โน้ตตอนรัน, ตัวตนที่แทนค่า, ตรวจพรอมป์, คัดลอกพรอมป์ที่ปิดข้อมูลลับ | เรียก API ผู้ดูแลเพื่อตรวจภาพรวมพรอมป์แบบปิดข้อมูลลับ, งบโทเคนรายส่วน, คลังความรู้ที่ดึงมาใช้, คำเตือน และส่วนต่างของพรอมป์ โดยไม่ยิงโมเดลจริง | รีเฟรช/เลือกตัวละคร/ตรวจพรอมป์มีเหตุผลบอกว่ากำลังโหลด, ยังไม่มีตัวละคร, ต้องมี `ADMIN_API_KEY`, ต้องเลือกตัวละคร, หรือกรอกข้อความก่อนตรวจ | แสดงสถานะยังไม่ได้ตรวจพรอมป์และบอกให้เลือกตัวละคร/ข้อความก่อนตรวจ |
| ชุดทดสอบอัตโนมัติ | `/admin/evals` | คีย์แอดมิน, ปุ่มรันชุดทดสอบ, แผงพับชุดทดสอบ, ผลเช็ก, สรุปจุดไม่ผ่าน | เรียก API ผู้ดูแลเพื่อรันชุด roleplay แบบผลซ้ำได้ ตรวจกฎคุมพรอมป์, คลังความรู้, ความต่อเนื่องของความสัมพันธ์/ฉาก และงบโทเคน โดยไม่ยิงโมเดลจริง | guard ด้วย `ADMIN_API_KEY`; ปุ่มรันชุดทดสอบมีเหตุผลบอกให้บันทึก key หรือรอรันให้เสร็จก่อน | แสดงสถานะยังไม่ได้รันชุดทดสอบ และอธิบายว่าจะเห็นชุดทดสอบ/ผลเช็ก/จุดไม่ผ่านหลังรัน |
| สเตจจิงจริง | โดเมนสเตจจิงจริง | Supabase จริง, ระบบหลังบ้านบน Render/Railway, โดเมนหน้าบ้าน, CORS จริง, e2e smoke | ต้องผ่านสเตจจิงก่อนขึ้นใช้งานจริง | เป็น checklist staging ไม่ใช่เมนูให้กด ต้องใช้บัญชีและโดเมนจริง | ใช้ `STAGING_RUNBOOK.md` |

Smoke เส้นทางอัตโนมัติล่าสุดเช็คทุก route หลักบนจอเดสก์ท็อปและมือถือ, ดักข้อผิดพลาดในคอนโซล/หน้าเบราว์เซอร์, และเช็คการล้นแนวนอนเพื่อจับปัญหาปุ่มล้นหรือหน้า admin ยิง API ทั้งที่ยังไม่มีสิทธิ์
เมนูแชทถูกเช็คทั้งสองพื้นผิวคือ sidebar ในห้องแชทและหน้า `/chats` รวมถึง pin/unpin, rename, archive, restore, delete, selection mode และ bulk archive/restore/delete

คำสั่ง smoke:

```bash
bun run route-menu:audit
bun run qa:seed
bun run e2e:smoke
bun run qa:full
```

Automated guard ล่าสุด:

- `scripts/route-menu-doc-check.ts` ตรวจให้ `ROUTE_MENU_AUDIT.md` มีทุก route/menu ที่แสดงใน `/admin/health`, route จริงใน `App.tsx`, nav path, และ route preload ครบตรงกัน ถ้าตารางหรือเมนู drift ออกจาก source จริงจะ fail ทันที
- `scripts/frontend-static-audit.ts` ตรวจปุ่ม `<button>` ที่ไม่มี `type`, ปุ่มไอคอนที่ไม่มี `aria-label`/`title`, ลิงก์ตัวอย่าง `href="#"`/`to="#"`, ตัวจัดการคลิกว่าง, ข้อความตัวอย่างที่ไม่ควรหลุดขึ้นก่อนปล่อยใช้งานจริง, และการเข้ารหัสข้อความที่เพี้ยน
- `scripts/frontend-route-audit.ts` ตรวจลิงก์/เมนู static ที่ชี้ไป route จริงใน `App.tsx` เพื่อกันเมนูที่กดแล้วตัน
- `scripts/check-frontend-bundles.ts` ล็อก bundle budget โดยเฉพาะ main bundle และหน้า Chat ที่ถูก lazy load
